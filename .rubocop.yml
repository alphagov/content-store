inherit_gem:
  rubocop-govuk:
    - config/default.yml
    - config/rails.yml

# Don't quite know how to fix this, so ignore it for now
Lint/UriEscapeUnescape:
  Enabled: false

# Mongoid, not ActiveRecord, makes RuboCop's suggestions for the
# following two cops not pass tests.
Rails/FindBy:
  Enabled: false

Rails/DynamicFindBy:
  Enabled: false
